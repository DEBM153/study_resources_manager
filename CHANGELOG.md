# 更新日志

## 当前版本：模块化命令行版本

本版本在第一版功能基础上完成了代码结构模块化，程序的命令行交互、数据格式和业务逻辑保持不变。

### 结构调整

- 新增 `app/` 包，用于拆分程序职责。
- 将菜单显示逻辑移动到 `app/menu.py`。
- 将 JSON 数据读取和保存逻辑移动到 `app/storage.py`。
- 将学习资料的添加、查询、查看、标签检索、自动整理、统计和删除逻辑移动到 `app/resources.py`。
- `main.py` 简化为程序入口和菜单调度循环。

### 保持不变

- 仍然通过终端菜单运行。
- 仍然使用 `data/resources.json` 保存学习资料。
- 资料数据结构保持为 `name`、`subject`、`path`、`tag`。
- 原有菜单编号、输入提示和输出文案保持不变。
- 原有功能流程保持不变，包括添加、搜索、查看、按标签查询、自动整理、统计、删除和退出。

## 第一版：命令行学习资料管理器

第一版完成了 StudyVault 的基础命令行功能。

### 初始功能

- 添加学习资料记录，包括资料名称、科目、路径和标签。
- 按 `name`、`subject`、`path`、`tag` 字段搜索资料。
- 查看全部学习资料。
- 按标签查询资料。
- 自动整理资料记录，包括清理空格、去重标签、排序标签和按科目排序。
- 统计科目数量、标签数量以及各分类下的资料数量。
- 删除指定资料记录，并在删除前进行确认。
- 使用 `data/resources.json` 进行本地持久化保存。

### 项目基础

- 建立 `README.md`，说明项目用途、运行方式、数据格式和功能。
- 建立 `.gitignore`，忽略 Python 缓存、虚拟环境和编辑器临时文件。
- 建立 `data/functions_menus.txt`，将菜单文本独立保存。
